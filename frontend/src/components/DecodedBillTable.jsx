/**
 * DecodedBillTable.jsx
 * Renders parsed line items from the bill in a readable, structured table.
 * Shows procedure codes, descriptions, amounts, and quantities.
 */

export default function DecodedBillTable() {
  return null; // TODO: Implement table with parsed bill line items
}
