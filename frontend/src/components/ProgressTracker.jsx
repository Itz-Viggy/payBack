/**
 * ProgressTracker.jsx
 * Step-by-step progress indicator shown while the bill is being analyzed.
 * Displays upload → extraction → flagging → ready states.
 */

export default function ProgressTracker() {
  return null; // TODO: Implement step-by-step progress UI
}
