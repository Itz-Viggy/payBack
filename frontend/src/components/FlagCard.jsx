/**
 * FlagCard.jsx
 * Card component displaying a single flagged charge with its reason and severity.
 * Used in the dispute flow to show why a charge was flagged (e.g. upcoding, duplicate).
 */

export default function FlagCard() {
  return null; // TODO: Implement flagged charge card with reason and severity
}
